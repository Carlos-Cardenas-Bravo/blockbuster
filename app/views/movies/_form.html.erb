<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
  <div class="notification is-danger">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name, style: "display: block" %>
    <div class="control">
      <%= form.text_field :name %>
    </div>
  </div>

  <h3>Clientes que han arrendado esta película</h3>
  <div>
    <%= form.fields_for :clients do |client_form| %>
      <div class="field">
        <%= client_form.label :name, style: "display: block" %>
        <div class="control">
          <%= client_form.text_field :name %>
        </div>
      </div>
      <div class="field">
        <%= client_form.label :age, style: "display: block" %>
        <div class="control">
          <%= client_form.text_field :age %>
        </div>
      </div>
      <div class="field">
        <%= client_form.check_box :_destroy %>
        <div class="control">
          <%= client_form.label :_destroy, "eliminar Cliente" %>
        </div>
      </div>
    <% end %>
  </div>

  <div class="field">
    <div class="control">
      <%= form.submit %>
    </div>
  </div>
<% end %>
